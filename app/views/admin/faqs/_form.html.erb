<%= form_with(model: admin_faq, url: admin_faqs_url) do |form| %>
  <% if admin_faq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_faq.errors.count, "error") %> prohibited this admin_faq from being saved:</h2>

      <ul>
        <% admin_faq.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

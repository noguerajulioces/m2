= form_with(model: admin_faq, url: admin_faqs_url) do |form|
  - if admin_faq.errors.any?
    #error_explanation
      %h2
        = pluralize(admin_faq.errors.count, "error")
        prohibited this admin_faq from being saved:
      %ul
        - admin_faq.errors.each do |error|
          %li= error.full_message
  .actions
    = form.submit

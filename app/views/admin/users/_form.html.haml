= form_with(model: admin_user) do |form|
  - if admin_user.errors.any?
    #error_explanation
      %h2
        = pluralize(admin_user.errors.count, "error")
        prohibited this admin_user from being saved:
      %ul
        - admin_user.errors.each do |error|
          %li= error.full_message
  .actions
    = form.submit
